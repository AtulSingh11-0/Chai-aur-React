import { createContext } from "react";

// Create a UserContext with default values for username and password
const UserContext = createContext();

// Export the UserContext to be used in other parts of the application
export default UserContext;
